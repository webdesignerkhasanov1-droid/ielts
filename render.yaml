services:
  # ── BITTA SERVIS: Frontend + Backend + Telegram Bot ──
  - type: web
    name: ielts-app
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node server.cjs
    envVars:
      - key: NODE_ENV
        value: production
